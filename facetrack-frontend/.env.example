# Frontend API Configuration
# Points to the nginx reverse proxy server with HTTPS
# The frontend uses this URL to make API requests to the Django backend
VITE_API_URL=https://192.168.100.90/api

# Optional: API request timeout in milliseconds
# VITE_API_TIMEOUT=30000
